// src/modules/User/pages/Settings.tsx
import React from 'react';
import {Card} from '../../../../common/components/ui/Card.tsx';

// Yalnızca içerik render edilir, Layout içindeki <Sidebar> ve <Topbar> kaldırıldı.
const UserSettings: React.FC = () => (
    <Card title="Ayarlar">
        {/* Burada kullanıcı ayarları formu yer alacaktır */}
    </Card>
);

export default UserSettings;